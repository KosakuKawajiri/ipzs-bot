- name: Commit aggiorna seen.txt se ci sono novità
  run: |
    git config --global user.name "github-actions"
    git config --global user.email "actions@github.com"
    git add seen.txt
    git diff --cached --quiet || git commit -m "Aggiorna seen.txt"
    git push
